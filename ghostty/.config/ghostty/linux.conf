# Linux-specific Ghostty config

# Use xterm-256color so remote servers (Ubuntu etc.) have the terminfo.
# Ghostty's own terminfo (xterm-ghostty) is rarely available on remote systems.
term = xterm-256color

# GTK window styling
gtk-toolbar-style = flat
