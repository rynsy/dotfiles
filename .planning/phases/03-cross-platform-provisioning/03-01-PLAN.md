---
phase: 03-cross-platform-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shell/.config/shell/config.d/alias
  - shell/.config/shell/config.d/alias.linux
  - shell/.config/shell/config.d/alias.darwin
  - shell/.config/shell/config.d/env
  - shell/.config/shell/config.d/env.linux
  - shell/.config/shell/config.d/env.darwin
  - shell/.config/shell/config.d/path
  - shell/.config/shell/config.d/path.linux
  - shell/.config/shell/config.d/path.darwin
  - shell/.config/shell/config.d/secrets.encrypted
  - shell/.config/shell/config.d/.gitignore
  - zsh/.config/zsh/.zshrc
  - zsh/.config/zsh/config.d/alias
  - zsh/.config/zsh/config.d/env
  - zsh/.config/zsh/config.d/path
  - zsh/.config/zsh/config.d/secrets.encrypted
  - zsh/.config/zsh/config.d/.gitignore
  - bash/.bashrc
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02

must_haves:
  truths:
    - "Shared shell config lives in shell/.config/shell/config.d/ with alias, env, path, secrets.encrypted"
    - "Linux-only entries (LUA_PATH, XDG_SESSION_TYPE, nmcli, navicat, zig-wasm, emscripten, Factorio) are in .linux platform files"
    - "macOS entries (brew shellenv, ls -G) are in .darwin platform files"
    - "Zsh sources from ~/.config/shell/config.d/ instead of ~/.config/zsh/config.d/"
    - "Bash sources from ~/.config/shell/config.d/ instead of ~/.config/zsh/config.d/"
    - "Platform-specific files sourced conditionally via uname"
    - "Old config.d files removed from zsh/ package (only zsh-specific files remain)"
  artifacts:
    - path: "shell/.config/shell/config.d/alias"
      provides: "Universal aliases (ls --color moved to alias.linux per research open question)"
      contains: "alias n=nvim"
    - path: "shell/.config/shell/config.d/env"
      provides: "Universal env vars"
      contains: "export EDITOR=nvim"
    - path: "shell/.config/shell/config.d/path"
      provides: "Universal PATH entries"
      contains: "export PATH=\"$HOME/.local/bin"
    - path: "shell/.config/shell/config.d/path.linux"
      provides: "Linux-only paths"
      contains: "emscripten"
    - path: "shell/.config/shell/config.d/path.darwin"
      provides: "macOS paths"
      contains: "brew shellenv"
    - path: "shell/.config/shell/config.d/env.linux"
      provides: "Linux-only env vars"
      contains: "XDG_SESSION_TYPE"
    - path: "zsh/.config/zsh/.zshrc"
      provides: "Updated sourcing block"
      contains: "~/.config/shell/config.d"
    - path: "bash/.bashrc"
      provides: "Updated sourcing block"
      contains: "~/.config/shell/config.d"
  key_links:
    - from: "zsh/.config/zsh/.zshrc"
      to: "shell/.config/shell/config.d/*"
      via: "source with uname case statement"
      pattern: "config/shell/config.d"
    - from: "bash/.bashrc"
      to: "shell/.config/shell/config.d/*"
      via: "dot-source with uname case statement"
      pattern: "config/shell/config.d"
---

<objective>
Extract shared shell configuration from `zsh/.config/zsh/config.d/` into a neutral `shell/.config/shell/config.d/` stow package, split platform-specific entries into `.linux` and `.darwin` override files, and update zsh and bash to source from the new location with `uname`-conditional platform loading.

Purpose: This is the foundation for cross-platform provisioning. All other plans depend on the shared config living in a shell-neutral location that any shell (zsh, bash, pwsh) can reference.
Output: `shell/` stow package with split config files; updated `.zshrc` and `.bashrc` sourcing blocks; old config.d files removed from `zsh/`.
</objective>

<execution_context>
@/home/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cross-platform-provisioning/03-CONTEXT.md
@.planning/phases/03-cross-platform-provisioning/03-RESEARCH.md

@zsh/.config/zsh/.zshrc
@zsh/.config/zsh/config.d/alias
@zsh/.config/zsh/config.d/env
@zsh/.config/zsh/config.d/path
@bash/.bashrc

<interfaces>
<!-- Current source files to extract from -->

From zsh/.config/zsh/config.d/alias (full content moves to shell, linux-only entries split out):
- Linux-only: wifi (nmcli), navicat, zig-wasm
- Universal: everything else
- Research open question: `alias ls='ls --color'` should move to alias.linux; add `alias ls='ls -G'` to alias.darwin

From zsh/.config/zsh/config.d/env (full content moves to shell, linux-only entries split out):
- Linux-only: LUA_PATH, LUA_CPATH, XDG_SESSION_TYPE
- Universal: everything else (EDITOR, VISUAL, LS_COLORS, DOTNET_*, VCPKG_*, ZMK BLE)

From zsh/.config/zsh/config.d/path (full content moves to shell, linux-only entries split out):
- Linux-only: emscripten (/lib64/emscripten), Factorio (/usr/local/games/factorio)
- Universal: ~/.local/bin, GEM_HOME, Composer, Dotnet, Pulumi, Cargo, Go

From zsh/.config/zsh/config.d/secrets.encrypted:
- Moves to shell/ as-is (ansible-vault encrypted)

Platform sourcing pattern (from 03-RESEARCH.md):
```bash
_shell_config="$HOME/.config/shell/config.d"
for f in env path alias; do
  [[ -f "$_shell_config/$f" ]] && . "$_shell_config/$f"
done
case "$(uname -s)" in
  Linux)  for f in env.linux path.linux alias.linux; do [[ -f "$_shell_config/$f" ]] && . "$_shell_config/$f"; done ;;
  Darwin) for f in env.darwin path.darwin alias.darwin; do [[ -f "$_shell_config/$f" ]] && . "$_shell_config/$f"; done ;;
esac
[[ -f "$_shell_config/secrets" ]] && . "$_shell_config/secrets"
unset _shell_config
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shell/ stow package with split config files</name>
  <files>
    shell/.config/shell/config.d/alias
    shell/.config/shell/config.d/alias.linux
    shell/.config/shell/config.d/alias.darwin
    shell/.config/shell/config.d/env
    shell/.config/shell/config.d/env.linux
    shell/.config/shell/config.d/env.darwin
    shell/.config/shell/config.d/path
    shell/.config/shell/config.d/path.linux
    shell/.config/shell/config.d/path.darwin
    shell/.config/shell/config.d/secrets.encrypted
    shell/.config/shell/config.d/.gitignore
  </files>
  <action>
Create the `shell/.config/shell/config.d/` directory structure and populate it by splitting the current zsh config.d files:

**alias -> alias (universal) + alias.linux + alias.darwin:**
- Universal `alias`: Keep termbin, gcm, gcl, python, pip, yarn, n, sail, killemall, t, ta, lab, cdar, kdai, proj, dui, study, s, p, dc. REMOVE `alias ls='ls --color'` from universal (it's Linux-specific GNU ls syntax).
- `alias.linux`: wifi (nmcli), navicat, zig-wasm, AND `alias ls='ls --color'` (GNU ls)
- `alias.darwin`: `alias ls='ls -G'` (BSD ls color flag)

**env -> env (universal) + env.linux + env.darwin:**
- Universal `env`: LS_COLORS, DOTNET_CLI_TELEMETRY_OPTOUT, VCPKGROOT, VCPKG_DISABLE_METRICS, CONFIG_ZMK_BLE_*, EDITOR, VISUAL
- `env.linux`: LUA_PATH, LUA_CPATH, XDG_SESSION_TYPE
- `env.darwin`: empty file with comment `# macOS-specific env vars` (placeholder for future use)

**path -> path (universal) + path.linux + path.darwin:**
- Universal `path`: ~/.local/bin, GEM_HOME + bin, Composer, Dotnet (DOTNET_ROOT + tools), Pulumi, Cargo, Go
- `path.linux`: emscripten (/lib64/emscripten), Factorio (/usr/local/games/factorio)
- `path.darwin`: `eval "$(/opt/homebrew/bin/brew shellenv 2>/dev/null)" || true`

**secrets.encrypted:** Copy from `zsh/.config/zsh/config.d/secrets.encrypted` (binary, ansible-vault).

**.gitignore:** Create with content to ignore the decrypted `secrets` file:
```
secrets
```

Verify the `.gitignore` at repo root already has `**/config.d/secrets` — this glob will match the new location too.
  </action>
  <verify>
    <automated>test -f shell/.config/shell/config.d/alias && test -f shell/.config/shell/config.d/env && test -f shell/.config/shell/config.d/path && test -f shell/.config/shell/config.d/alias.linux && test -f shell/.config/shell/config.d/env.linux && test -f shell/.config/shell/config.d/path.linux && test -f shell/.config/shell/config.d/path.darwin && test -f shell/.config/shell/config.d/secrets.encrypted && grep -q "alias n=nvim" shell/.config/shell/config.d/alias && grep -q "nmcli" shell/.config/shell/config.d/alias.linux && grep -q "EDITOR=nvim" shell/.config/shell/config.d/env && grep -q "XDG_SESSION_TYPE" shell/.config/shell/config.d/env.linux && grep -q "emscripten" shell/.config/shell/config.d/path.linux && grep -q "brew shellenv" shell/.config/shell/config.d/path.darwin && ! grep -q "ls --color" shell/.config/shell/config.d/alias && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - shell/.config/shell/config.d/ contains: alias, alias.linux, alias.darwin, env, env.linux, env.darwin, path, path.linux, path.darwin, secrets.encrypted, .gitignore
    - Universal files have NO Linux-only entries (no nmcli, LUA_PATH, XDG_SESSION_TYPE, emscripten, Factorio, ls --color)
    - Platform files contain the correct platform-specific entries
    - `alias ls='ls --color'` is in alias.linux, `alias ls='ls -G'` is in alias.darwin
  </done>
</task>

<task type="auto">
  <name>Task 2: Update zsh and bash sourcing, remove old config.d files</name>
  <files>
    zsh/.config/zsh/.zshrc
    bash/.bashrc
    zsh/.config/zsh/config.d/alias
    zsh/.config/zsh/config.d/env
    zsh/.config/zsh/config.d/path
    zsh/.config/zsh/config.d/secrets.encrypted
    zsh/.config/zsh/config.d/.gitignore
  </files>
  <action>
**Update `.zshrc`:** Replace the current sourcing block (lines 72-75 in the current file):
```
[[ ! -f "$HOME/.config/zsh/config.d/alias" ]] || source "$HOME/.config/zsh/config.d/alias"
[[ ! -f "$HOME/.config/zsh/config.d/env" ]] || source "$HOME/.config/zsh/config.d/env"
[[ ! -f "$HOME/.config/zsh/config.d/path" ]] || source "$HOME/.config/zsh/config.d/path"
[[ ! -f "$HOME/.config/zsh/config.d/secrets" ]] || source "$HOME/.config/zsh/config.d/secrets"
```

With the platform-conditional sourcing block:
```bash
# Source shared shell config (shared with bash)
_shell_config="$HOME/.config/shell/config.d"
for f in env path alias; do
  [[ -f "$_shell_config/$f" ]] && source "$_shell_config/$f"
done
case "$(uname -s)" in
  Linux)  for f in env.linux path.linux alias.linux; do [[ -f "$_shell_config/$f" ]] && source "$_shell_config/$f"; done ;;
  Darwin) for f in env.darwin path.darwin alias.darwin; do [[ -f "$_shell_config/$f" ]] && source "$_shell_config/$f"; done ;;
esac
[[ -f "$_shell_config/secrets" ]] && source "$_shell_config/secrets"
unset _shell_config
```

Keep all other .zshrc content (p10k, zinit, history, completion styling, shell integrations) unchanged.

**Update `.bashrc`:** Replace the current sourcing block (lines 13-15):
```
[[ -f "$HOME/.config/zsh/config.d/env"   ]] && . "$HOME/.config/zsh/config.d/env"
[[ -f "$HOME/.config/zsh/config.d/path"  ]] && . "$HOME/.config/zsh/config.d/path"
[[ -f "$HOME/.config/zsh/config.d/alias" ]] && . "$HOME/.config/zsh/config.d/alias"
```

With the same platform-conditional sourcing block (use `. ` instead of `source` for POSIX compatibility, though both work in bash):
```bash
# Source shared config (single source of truth; also used by zsh)
_shell_config="$HOME/.config/shell/config.d"
[[ -f "$_shell_config/env"   ]] && . "$_shell_config/env"
[[ -f "$_shell_config/path"  ]] && . "$_shell_config/path"
[[ -f "$_shell_config/alias" ]] && . "$_shell_config/alias"
case "$(uname -s)" in
  Linux)  for f in env.linux path.linux alias.linux; do [[ -f "$_shell_config/$f" ]] && . "$_shell_config/$f"; done ;;
  Darwin) for f in env.darwin path.darwin alias.darwin; do [[ -f "$_shell_config/$f" ]] && . "$_shell_config/$f"; done ;;
esac
[[ -f "$_shell_config/secrets" ]] && . "$_shell_config/secrets"
unset _shell_config
```

**Remove old config.d files from zsh/:** Delete the following files that have been extracted to shell/:
- `zsh/.config/zsh/config.d/alias`
- `zsh/.config/zsh/config.d/env`
- `zsh/.config/zsh/config.d/path`
- `zsh/.config/zsh/config.d/secrets.encrypted`
- `zsh/.config/zsh/config.d/.gitignore`

Use `git rm` for tracked files so the deletion is staged. The `zsh/.config/zsh/config.d/` directory can remain if zsh-specific configs are added later, but should be empty or removed if nothing remains.

**Migration safety (per pitfall #1 from RESEARCH.md):** The sequence is: (1) shell/ package already created in Task 1, (2) source paths updated here, (3) old files removed here. This ensures no dangling symlinks.
  </action>
  <verify>
    <automated>grep -q "config/shell/config.d" zsh/.config/zsh/.zshrc && grep -q "config/shell/config.d" bash/.bashrc && ! grep -q "config/zsh/config.d" zsh/.config/zsh/.zshrc && ! grep -q "config/zsh/config.d" bash/.bashrc && ! test -f zsh/.config/zsh/config.d/alias && ! test -f zsh/.config/zsh/config.d/env && ! test -f zsh/.config/zsh/config.d/path && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - .zshrc sources from ~/.config/shell/config.d/ with uname-conditional platform loading
    - .bashrc sources from ~/.config/shell/config.d/ with uname-conditional platform loading
    - No references to ~/.config/zsh/config.d/ remain in .zshrc or .bashrc
    - Old config.d files (alias, env, path, secrets.encrypted, .gitignore) removed from zsh/ package
    - `stow -n --no-folding shell` dry-run passes (no conflicts)
  </done>
</task>

</tasks>

<verification>
1. `stow -n --no-folding shell` -- dry-run shows no conflicts
2. `bash -c '. shell/.config/shell/config.d/env && echo $EDITOR'` -- prints "nvim"
3. `bash -c '. shell/.config/shell/config.d/alias && type n'` -- shows alias
4. `grep -c "config/zsh/config.d" zsh/.config/zsh/.zshrc bash/.bashrc` -- returns 0 (no old references)
5. `ls shell/.config/shell/config.d/` -- shows all expected files
</verification>

<success_criteria>
- shell/ stow package exists with all config files split into universal + platform overrides
- zsh and bash both source from the new ~/.config/shell/config.d/ location
- Platform-specific entries are gated by uname (Linux/Darwin)
- No old config files remain in zsh/.config/zsh/config.d/
- `alias ls='ls --color'` is Linux-only; `alias ls='ls -G'` is macOS-only
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-platform-provisioning/03-01-SUMMARY.md`
</output>
