---
phase: 04-retroactive-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-zmk-firmware/02-VERIFICATION.md
autonomous: true
requirements: [ZMK-01, ZMK-02, ZMK-03, ZMK-04, ZMK-05]

must_haves:
  truths:
    - "02-VERIFICATION.md exists in the Phase 02 directory"
    - "All 5 Phase 02 requirement IDs appear in the requirements coverage table"
    - "Each requirement has a status (PASS, FAIL, PARTIAL) backed by codebase evidence"
    - "ZMK-03 and ZMK-04 are marked FAIL with clear evidence that Layer 5 Android does not exist"
    - "ZMK-05 is marked PASS or PARTIAL with note about CI build deferral"
  artifacts:
    - path: ".planning/phases/02-zmk-firmware/02-VERIFICATION.md"
      provides: "Retroactive verification report for Phase 02"
      contains: "Requirements Coverage"
  key_links:
    - from: "02-VERIFICATION.md requirements table"
      to: "REQUIREMENTS.md ZMK-* IDs"
      via: "requirement ID matching"
      pattern: "ZMK-0[1-5]"
---

<objective>
Verify all 5 Phase 02 (ZMK Firmware) requirements against the actual codebase and produce a retroactive VERIFICATION.md.

Purpose: Phase 02 was executed before the GSD verification workflow existed. The v1.0 milestone audit identified these requirements as lacking verification evidence. This plan inspects the keymap file and git history to produce evidence-backed verification. Research indicates ZMK-03 and ZMK-04 will likely FAIL (Android layer not found in keymap).

Output: `.planning/phases/02-zmk-firmware/02-VERIFICATION.md`
</objective>

<execution_context>
@/home/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-retroactive-verification/04-RESEARCH.md
@.planning/phases/03-cross-platform-provisioning/03-VERIFICATION.md
@.planning/phases/02-zmk-firmware/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather evidence for all 5 Phase 02 requirements</name>
  <files>None (read-only evidence gathering)</files>
  <action>
Inspect `zmk/config/adv360.keymap` and git history to gather evidence for each Phase 02 requirement. Do NOT modify any files.

**ZMK-01** (Media keys prev/play-pause/next on Fn layer):
- Read `zmk/config/adv360.keymap`, find Layer 2 (Fn layer)
- Search for `C_PREV`, `C_NEXT`, `C_PP` (play/pause)
- Record exact line numbers and positions in the layer
- Expected: all three media key codes present on Fn layer (HJKL row per ROADMAP notes)

**ZMK-02** (Volume keys up/down/mute on Fn layer):
- In same Layer 2, search for `C_VOL_DN`, `C_VOL_UP`, `C_MUTE`
- Record exact line numbers
- Expected: all three volume key codes present on Fn layer

**ZMK-03** (Layer 5 Android added with BT slot 3):
- Search entire keymap for "Layer 5", "Android", "layer 5", `&bt BT_SEL 3`
- Count total layers defined in the keymap
- Read the keymap header comment for layer index
- Expected per research: NOT FOUND. Keymap has only Layers 0-4. Mark FAIL.

**ZMK-04** (Mod+A toggles Layer 5):
- Search Layer 3 (Mod layer) for `&tog 5` binding
- Search for any `&tog 5` anywhere in the keymap
- Record what IS on the A key position in Layer 3
- Expected per research: NOT FOUND. Mod layer has `&tog 4` (Mac layer) on M key, no `&tog 5`. Mark FAIL.

**ZMK-05** (Firmware builds via GitHub Actions):
- Read `02-02-SUMMARY.md` for push confirmation
- Check if `gh` CLI is available and attempt to query GitHub Actions status for rynsy/Adv360-Pro-ZMK
- If gh available: `gh run list --repo rynsy/Adv360-Pro-ZMK --limit 5`
- If not available: mark PARTIAL based on SUMMARY evidence that push succeeded

Record all evidence with exact file paths and line numbers for Task 2.
  </action>
  <verify>
    <automated>echo "Evidence gathering is read-only -- verify no files were modified" && cd /home/ryan/dotfiles && git diff --name-only | grep -v "claude/.claude/settings.json" | wc -l | grep -q "^0$" && echo "PASS: no files modified"</automated>
  </verify>
  <done>Evidence gathered for all 5 ZMK requirements with file paths, line numbers, keymap excerpts, and status determinations</done>
</task>

<task type="auto">
  <name>Task 2: Write 02-VERIFICATION.md</name>
  <files>.planning/phases/02-zmk-firmware/02-VERIFICATION.md</files>
  <action>
Create `.planning/phases/02-zmk-firmware/02-VERIFICATION.md` using the Phase 03 VERIFICATION.md format (at `.planning/phases/03-cross-platform-provisioning/03-VERIFICATION.md`).

Structure:
1. YAML frontmatter: phase, verified timestamp, status, score
2. Phase Goal from ROADMAP: "The keyboard firmware has media/volume keys on the Fn layer and a working Android BT layer, built and flashed successfully"
3. Observable Truths table (from ROADMAP success criteria or derived from requirements)
4. Required Artifacts table (keymap file, layer definitions)
5. Key Link Verification table (keymap -> firmware build)
6. Requirements Coverage table -- ALL 5 IDs with evidence from Task 1
7. Anti-Patterns Found section
8. Human Verification Required section -- ZMK-05 CI build check, ZMK-03/04 Android layer decision
9. Gaps Summary

Key notes for specific requirements:
- **ZMK-01**: Expected PASS. Media keys present on Fn layer HJKL row.
- **ZMK-02**: Expected PASS. Volume keys present on Fn layer.
- **ZMK-03**: Expected FAIL. Layer 5 (Android) does not exist in keymap. Note this in gaps summary. The ROADMAP claims completion, but the keymap has only 5 layers (0-4). Include the possible explanations from research: never added, merged into Mac layer concept, or requirement diverged from implementation.
- **ZMK-04**: Expected FAIL. No `&tog 5` binding exists. Mod layer has `&tog 4` on M key (Mac layer toggle). Note in gaps summary.
- **ZMK-05**: Mark based on evidence available. If gh CLI shows green build, PASS. If only SUMMARY evidence, PARTIAL with note.

Overall status logic:
- With ZMK-03 and ZMK-04 as FAIL: status = partial
- Score = 3/5 (or adjusted based on actual findings)

In Gaps Summary, clearly document:
1. ZMK-03 and ZMK-04 are unimplemented requirements (Android layer)
2. Whether these are candidates for a future phase or should be descoped
3. ZMK-05 CI verification status
  </action>
  <verify>
    <automated>test -f /home/ryan/dotfiles/.planning/phases/02-zmk-firmware/02-VERIFICATION.md && grep -c "ZMK-0[1-5]" /home/ryan/dotfiles/.planning/phases/02-zmk-firmware/02-VERIFICATION.md | grep -q "^5$" && echo "PASS: file exists with all 5 requirement IDs" || echo "FAIL: missing file or requirement IDs"</automated>
  </verify>
  <done>02-VERIFICATION.md exists with all 5 requirement IDs, each with PASS/FAIL/PARTIAL status and codebase evidence (file paths, line numbers, keymap excerpts). ZMK-03 and ZMK-04 documented as FAIL with clear gap explanation.</done>
</task>

</tasks>

<verification>
- 02-VERIFICATION.md exists at `.planning/phases/02-zmk-firmware/02-VERIFICATION.md`
- All 5 requirement IDs (ZMK-01 through ZMK-05) appear in the requirements coverage table
- Each requirement has evidence-backed status
- ZMK-03 and ZMK-04 are documented as FAIL with explanation
- Format matches Phase 03 VERIFICATION.md structure
</verification>

<success_criteria>
- Phase 02 VERIFICATION.md produced with evidence for all 5 requirements
- No code files were modified (this is a verification-only plan)
- Each requirement status is backed by keymap file evidence (line numbers, excerpts)
- Gaps (ZMK-03, ZMK-04) clearly documented with context
</success_criteria>

<output>
After completion, create `.planning/phases/04-retroactive-verification/04-02-SUMMARY.md`
</output>
